你作为AI，帮我生成code需要遵循以下规则：
1、首先这个是一个python工程，所以我采用了UV作为项目管理，你需要的所有python module必须安装到当前project下的venv中，并且使用里面的uv run进行项目验证
2、代码和注释，或者文档都必须使用英文
3、如果需要将当前工程进行打包可以使用全局环境的pyinstaller，但是打包的内容必须是当前环境里的module，或者你可以在当前uv 里面安装pyinstaller，但是用完需要删除
4、每次生成代码后都给我进行简单的讲解，采用中英文的方式

上面是你需要遵守的一些编程规则，下面呢，是这个工程具体是干啥的。
这是一个基于pygame的2D平面移动引擎，其核心思想是，基于pygame的2D功能，创建自定义组件
1、有的组件可以在当前平面自由移动，可以拖拽，可以放大放小
2、组件可以通过连线互相连接
3、可以通过保存的方法，将当前组件的位置进行保存，下次load的时候可以自动加载回来
4、先创建一个圆和长方形，作为基础组件